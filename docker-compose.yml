services:
  bot:
    build:
      context: .
      network: host
    env_file: .env
    environment:
      DB_PATH: /app/data/members.db
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
